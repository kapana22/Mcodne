# Deep audit — 2026-07-27

4-dimension audit (functional bugs, Georgian copy, security, performance). All items below are **fixed** in the working tree, `npx tsc --noEmit` is clean, and **no schema change is required** (no `prisma db push`). Not yet deployed.

## Fixed

| # | Severity | Area | Problem | Fix |
|---|----------|------|---------|-----|
| 1 | 🔴 HIGH | Security | Stored XSS: JSON-LD `dangerouslySetInnerHTML` didn't escape `</script>`; an expert's self-edited `fullName`/`bio`/`specialty` could break out and run JS in any visitor's (incl. admin's) browser. | New `lib/jsonLd.ts` `jsonLdString()` escapes `<`/`>`/`&`; wired into all 6 injection sites. |
| 2 | 🟠 | Booking | Dual-role user could double-book themselves (self-overlap check only matched `studentId`, missed their expert-side sessions under `tutor.userId`). | Union both hats in `bookings` create + reschedule accept. |
| 3 | 🟠 | Booking | Abandoned reschedule proposal froze a booking in PREPARING forever and locked its slot (cleanup exempted every pending-reschedule row). | Exempt only fresh + actionable proposals (48h TTL, future time); stale ones auto-cancel. |
| 4 | 🟠 | Availability | Manually-added multi-hour slot sold only one session (whole block marked booked). | Server splits the range into session-length slots (cap 96). |
| 5 | 🟠 | Booking | Rejecting a reschedule after the booking was separately accepted left a permanent "pending reschedule" banner (reject matched 0 rows). | CASE update clears the blob for PREPARING+CONFIRMED, never downgrades CONFIRMED. |
| 6 | 🟡 | Onboarding | "Needs revision" re-edit was a blank form off-device / after the 7-day draft expired. | `GET /api/applications` returns the applicant's own text fields; wizard seeds from them (draft wins). Removed dead `personalId/dob/bank/iban`. |
| 7 | 🟡 | Performance | 4 list routes shipped `professionData` + legacy base64 `videoUrl` across the DB→app wire per row. | `omit` both at the Prisma level; narrowed messages-POST tutor fetch. |
| 8 | 🟡 | Security | IP rate-limits bypassable via spoofed `X-Forwarded-For` (leftmost hop). | `clientIp()` keys on the proxy-appended hop (`TRUSTED_PROXY_HOPS`, default 1). |
| — | 🟢 | Georgian | `сцадეთ`→`სცადე`; ASCII `"`→`“` (U+201C) in blog delete dialog; 8 placeholder `...`→`…`. | Done. |

## Deliberately left (operator action)

- **Cron secret in URL query** (`?secret=` on cleanup/reminders): the live Railway */15 cron uses it, so removing it would break cleanup+reminders until the cron command is switched to an `Authorization: Bearer` header. Operator TODO (rotate + move to header).

## False positive

- disputes `#chat` anchor — the `<section id="chat">` contract still exists on the tutor booking detail page. No change.
