# აუდიტი — მომხმარებელთა კატეგორიები: რეგისტრაცია → ადმინი → ბარათი
**2026-08-11 · წყარო: კოდი + ცოცხალი ბაზა (read-only queries)**

## 0. რა არის ცოცხლად ახლა

| | რიცხვი |
| --- | --- |
| STUDENT | 26 (24 განაცხადის გარეშე, 2 უარყოფილი) |
| TUTOR | 22 |
| ADMIN | 2 (ერთ მათგანს აქვს TutorProfile) |
| განაცხადები | 22 APPROVED · 2 REJECTED · **0 რიგში** · 0 NEEDS_REVISION |
| ბრაუზში ჩანს | 20 პროფილი |
| სფეროების ჯამი (ჩიპებზე) | **19** |

**რეგისტრაციის ერთადერთი გზა ექსპერტისთვის:** `/signup` („ვარ ექსპერტი" ღილაკი) → ანგარიში იქმნება **STUDENT-ად** → `/apply` → 2 ეკრანი → ადმინის დამტკიცება → `role = TUTOR` + `TutorProfile`.

**კატეგორია სამ ადგილას ცხოვრობს და სამი სხვადასხვა რამეა:**
1. `TutorApplication.specialty` — **თავისუფალი ტექსტი**, განაცხადზე არჩეული ჩიპის სახელი, გაყინული იმ დღის სახელით (`categoryId` განაცხადის მოდელში **საერთოდ არ არსებობს**).
2. `TutorProfile.categoryId` — რეალური FK, რომელსაც დამტკიცება **სტრიქონების შედარებით** ამოიცნობს.
3. `TutorProfile.specialty` — `specialty`-ს ასლი, აღარ რედაქტირდება, მაგრამ სტუდენტის ეკრანები სწორედ მას ბეჭდავენ.

---

## კრიტიკული

### C1. „ფინანსები" „გადასახადებს" ეკუთვნის, არა „ბიზნესსა და ფინანსებს"
ბაზაში `finance.parentId` = **`tax`**-ის id. მიგრაცია (`2026-08-10-category-hierarchy/up.sql`) წერდა `('finance', 'business')` — ცოცხალი მდგომარეობა სხვაა.

შედეგი, ზუსტი რიცხვებით:

| სფერო | ვინ ჩანს |
| --- | --- |
| ბიზნესი და **ფინანსები** | 2 — მანუჩარ წურწუმია (ბიზნესი), ნინო გახოკია (**ფსიქოლოგი**) |
| გადასახადები | 5 — 3 საგადასახადო + **Nona Kvitsiani, თამარ ფხოველიშვილი (ფინანსები, 30 წელი)** |

სფერო, რომლის სახელშიც წერია „ფინანსები", ფინანსისტს არ შეიცავს. ორივე ჩიპი ცრუობს თავისი შიგთავსის შესახებ.

### C2. `profileType`-ს არსად არავინ წერს — სასწავლო ვერტიკალი ცოცხალია და მიუწვდომელი
`PACKAGES_VISIBILITY = 'signed-in'` (lib/flags.ts) — ანუ ნებისმიერი შესული მომხმარებლისთვის ჩართულია. `/swavleba` ითხოვს `profileType: 'TEACHER'` **და** `packagesEnabled: true`.

`profileType`-ს არ ანიჭებს არაფერი: არც რეგისტრაცია, არც `/apply`, არც დამტკიცება, არც ადმინ პანელი (იქ მხოლოდ `packagesEnabled`-ის ჩამრთველია — `app/api/admin/tutors/[id]/packages/route.ts`). ბაზაში ერთადერთი TEACHER არის **„ლუკა კაპანაძე"**, რომელიც `scripts/seed-test-teacher.ts`-მა შექმნა: 0 სერვისი, კატეგორია `∅`, სპეციალობა „ინგლისური ენა".

ორი შედეგი:
- ადმინი ჩართავს „პაკეტებს" რეალურ ექსპერტს → ის გახსნის `/tutor/packages`-ს → `POST` აბრუნებს **403 NOT_TEACHER** („პაკეტს მხოლოდ მასწავლებელი ქმნის."). გამოსავალი არ არსებობს პროდუქტში.
- სატესტო seed-ჩანაწერი საჯაროდ ჩამოთვლილია `/swavleba`-ზე.

### C3. პროფილის რედაქტორი სთავაზობს კატეგორიებს, რომლებსაც API არ იღებს
`app/tutor/profile/_tabProfile.tsx` `<optgroup>`-ში ხატავს ქვე-მიმართულებებს. `/api/categories`-ის `children` **მხოლოდ `REDIRECTED`** რიგებია. `PATCH /api/me/tutor` კი იღებს მხოლოდ `status: 'VISIBLE'`-ს (გამონაკლისი — ის, რომელიც უკვე უჭირავს).

ანუ ექსპერტი, რომელიც აირჩევს **ფინანსები · გაყიდვები · ადვოკატი · კრიპტო · დიზაინი · პროდუქტი · კადრები** (15-დან 7 კატეგორია), იღებს `400 BAD_CATEGORY`. და რადგან `categoryId` იმავე PATCH-ში მიდის, სადაც სათაური, ბიო, ფასი და ენები — **მთელი შენახვა ვარდება**, ტოსტით „შენახვა ვერ მოხერხდა — სცადე თავიდან", კატეგორიის ხსენების გარეშე.

---

## მაღალი

### H1. ფსიქოლოგი ბიზნესის სფეროშია — და ადმინი მას ვერ ასწორებს
**ნინო გახოკია**: განაცხადი „ფსიქოლოგია", `specialty` „ფსიქოლოგია", `categoryId` → **„ბიზნესი და ფინანსები"**. ის არ არის ფსიქოლოგიის სფეროში (სადაც 2 ადამიანია), არის ბიზნესში (სადაც 2-ია).
- ბარათზე ჩიპი: „ბიზნესი და ფინანსები".
- პროფილზე **ორივე** ჩიპი: „ბიზნესი და ფინანსები" + „ფსიქოლოგია" — თვალსაჩინო წინააღმდეგობა.

**და გამოსწორება ადმინიდან შეუძლებელია:** `app/api/admin/users/[id]` და `app/api/admin/tutors/[id]/*` იძლევა მხოლოდ `verified` / `featured` / `packages` / `suspend` / `makeAdmin`-ს. კატეგორიის ცვლილების endpoint არ არსებობს. ერთადერთი გზა — თავად ექსპერტმა შეცვალოს თავის რედაქტორში (რომელიც C3-ის გამო ნახევრად გატეხილია).

### H2. დამტკიცების კატეგორიის ამომცნობი არადეტერმინისტულია
`app/api/applications/[id]/route.ts`:
```ts
const liveCats = await prisma.category.findMany({ where: { OR: [BROWSABLE_CATEGORY, { status: 'HIDDEN' }] }, … })
// ↑ orderBy არ არის — Postgres აბრუნებს ფიზიკური რიგის მიხედვით
… liveCats.find(c => { const n = nrm(c.name); const stem = n.slice(0, 4); return sp.includes(n) || n.includes(sp) || (stem.length >= 3 && sp.includes(stem)) })
```
- **`orderBy` არ არის** → როცა ორი კატეგორია ერთსა და იმავე ტექსტს ერგება, „პირველი" ფიზიკური რიგია. VACUUM-ის ან UPDATE-ის შემდეგ იგივე განაცხადი სხვა სფეროში მოხვდება.
- 4-სიმბოლოიანი ფუძე ხმაურიანია: „პროდ" (პროდუქტი) ერგება „პროდიუსერს", „კარი" (კარიერა) — „კარიერულს" და სხვას.
- `liveCats` შეიცავს **HIDDEN**-საც, მათ შორის `diaspora`-ს — იმ მარკერს, რომელსაც მიგრაცია ცალკე იცავს („a stray 'turn it on' in the panel cannot quietly put it in the menu"). დამტკიცება კი HIDDEN კატეგორიას **ავტომატურად აქცევს VISIBLE-ად**. ერთადერთი კარი, რომელიც ღიაა.

### H3. თავისუფალი ტექსტი ისე იხატება, თითქოს ჩვენი ტაქსონომია იყოს
`app/tutors/_data.tsx:181` — `cat: t.category?.name ?? t.specialty ?? 'სფერო'`.

**გვანცა ჭაღოშვილი**-ს კატეგორია `null`-ია, `specialty` — „ბუღალტერია". მისი ბარათი აჩვენებს ჩიპს „ბუღალტერია" **ზუსტად იმავე pill-ში**, რომელშიც რეალური სფერო. CLAUDE.md ცალკე იწერს, რომ ბარათი სწორედ ამის საწინააღმდეგოდ გადაკეთდა („it made an unvalidated string look like a platform-verified label") — fallback ამას აბრუნებს.

ამასთან ის არცერთ სფეროში არ ხვდება (`passesFilters` ადარებს `catSlug`-ს, რომელიც `null`-ია). აქედან: **/tutors აჩვენებს 20-ს, სფეროების ჩიპების ჯამი 19-ია.**

### H4. ორი ცრუ გაფრთხილება — ორივე მომხმარებელს ჩვენებული
- ადმინის მოდერაცია: „თუ კატეგორიას არ მიუთითებ, ექსპერტი /tutors-ზე არ გამოჩნდება" (`_moderation.tsx:859`)
- პროფილის რედაქტორი: „მის გარეშე პროფილი ვერ გამოჩნდება." (`_tabProfile.tsx:168`)

`lib/tutorsQuery.ts` პირიქით ამბობს, კოდშიც და კომენტარშიც: *„Category is a LABEL, not a gate… an expert whose category is unset stays visible"*. ცოცხალი მტკიცებულება — გვანცა კატეგორიის გარეშე ბრაუზში ჩანს. მოდერატორს ცრუ გაფრთხილება უბიძგებს **არასწორი** კატეგორია მიანიჭოს, ექსპერტს კი ეუბნება, რომ უჩინარია, როცა არ არის.

---

## საშუალო

### M1. სფეროების გადარქმევამ 9 პროფილზე დუბლი გამოაჩინა
`app/tutors/[id]/_hero.tsx:320` აჩვენებს `specialty`-ს მეორე ჩიპად, როცა ის კატეგორიის სახელს არ ემთხვევა — კომენტარით „On all nine live rows the two strings are IDENTICAL, so this swap is invisible today". 2026-08-10-ის გადარქმევის შემდეგ ეს აღარაა ასე:

| specialty | კატეგორია | პროფილები |
| --- | --- | --- |
| ბიზნესი / ბიზნეს-სტრატეგია | ბიზნესი და ფინანსები | 2 |
| IT | ტექნოლოგია და პროდუქტი | 2 |
| მარკეტინგი | მარკეტინგი და გაყიდვები | 4 |
| **ფსიქოლოგია** | **ბიზნესი და ფინანსები** | 1 (ნინო — წინააღმდეგობა) |

9 პროფილი ახლა ორ თითქმის-იდენტურ ჩიპს აჩვენებს.

### M2. მოდერატორის ჩამონათვალი და სერვერის ლოგიკა ორ ადგილას სცდება
- `matchCategory` (`_moderation.tsx:70`) **არ** შეიცავს სერვერის slug-არმს. განაცხადი specialty-ით „IT" პანელში იკითხება როგორც „ვერ დაემთხვა", თუმცა სერვერი მას სწორად ფაილავს.
- პანელის `liveCats` **გამორიცხავს HIDDEN-ს**, `/apply` კი სთავაზობს („კარიერა", „უძრავი ქონება", „რელოკაცია") და სერვერიც ამოიცნობს. ასეთი განაცხადი იღებს წითელ „ვერცერთ სფეროს ვერ დაემთხვა"-ს და **ჩუმად ამოვარდება bulk approve-იდან** (`askBulkApprove` → `catIdFor` → undefined).

### M3. მოდერატორის select ბრტყელია, ექსპერტისა — დაჯგუფებული
`_moderation.tsx:855` — `liveCats.map(c => <option>{c.name}</option>)`. სფეროები და ქვე-მიმართულებები ერთ სიაშია, უნიშნოდ: „ბიზნესი და ფინანსები", „ფინანსები", „კადრები", „გაყიდვები", „ადვოკატი" ერთმანეთის ტოლი ჩანს. რედაქტორი კი `<optgroup>`-ს იყენებს.

### M4. `specialty` გაყინული ისტორიაა, რომელსაც არაფერი ასინქრონებს
ის იწერება ერთხელ დამტკიცებაზე, აღარ რედაქტირდება (ველი რედაქტორიდან წაშლილია, თუმცა `page.tsx:301` მაინც აგზავნის უცვლელად) — და **სწორედ ის იხატება სტუდენტის ეკრანებზე**: `_discover.tsx:168`, `_next.tsx:74`, `_sessions.tsx:189`, `favorites/client.tsx:176`, `abroad`, `konsultacia`. ბარათი და პროფილი კი `category.name`-ს ხატავს. ერთი და იგივე ექსპერტი სტუდენტის შენახულებში „ბიზნესია", ბარათზე — „ბიზნესი და ფინანსები".

### M5. რეგისტრაციის განზრახვა არსად არ ინახება
„ვარ ექსპერტი" ტაბი `POST /api/auth/signup`-ს უგზავნის იმავე body-ს, რასაც სტუდენტი (`role` კლიენტიდან საერთოდ არ მიიღება), შემდეგ `window.location.href = '/apply'`. 24 ანგარიშს განაცხადი არ აქვს და **ბაზაში არაფერი ამბობს, რომელი მათგანი ექსპერტად შემოვიდა და ფორმაში ჩავარდა**. კითხვა „ვინ რაზე დარეგისტრირდა" მონაცემებიდან პასუხგაუცემელია; მხოლოდ apply-ის ანონიმური funnel events-ია.

### M6. 22 დამტკიცებიდან 0-ს აქვს „გადამოწმებული"
ერთადერთი `verified: true` არის ადმინის საკუთარი პროფილი (`luka kapanadze`, ლათინური სახელი, 0 სერვისი). ბეიჯი ყველა ბარათსა და პროფილზეა გათვალისწინებული და 22-ჯერ არავის მოუნიშნავს — ან ის მოდერაციის ჩვევის ნაწილი არაა, ან არ უნდა იყოს per-approval checkbox.

### M7. ADMIN-ს აქვს TutorProfile
დამტკიცების route ცალკე იცავს ADMIN-ს (`CANNOT_PROMOTE_ADMIN`), რადგან პრომოცია მას ადმინობას წაართმევს — რიგი მაინც არსებობს, `verified: true`-თი. ბრაუზიდან მას მხოლოდ 0 სერვისი აკავებს: **ერთი სერვისის დაშორებით საჯარო ჩამონათვალშია**.

---

## დაბალი

- `/api/tutors?type=CONSULTATION|RECURRING` საჯარო პარამეტრია UI-ს გარეშე. `serviceType` = 12 RECURRING / 10 CONSULTATION — მკვდარი legacy (თავად `lib/flags.ts` ამბობს). ვინც ამ პარამეტრს დაარტყამს, სიის ნახევრად შემთხვევით ნახევარს მიიღებს.
- `Category.order` იმეორებს: tax=2 / finance=2, marketing=5, sales=6 / psychology=6, advokati=**901**.
- „ადვოკატი" — პიროვნების აღმნიშვნელი სახელი დარგების სიაში (დანარჩენი ყველა დარგია).
- გიორგი მელაძე: `suspendedAt` დაყენებულია, `available` კვლავ `true` — ორი ველი ერთმანეთს ეწინააღმდეგება (დამალვა suspend-ზე მუშაობს).
- წითელი „«X» ვერცერთ სფეროს ვერ დაემთხვა" ჩნდება მაშინაც, როცა მოდერატორმა თავად გაასუფთავა select.
- 22 APPROVED / 2 REJECTED / 0 NEEDS_REVISION — 92% დამტკიცება, „შესწორება" არასდროს გამოყენებულა.

---

## რიგითობა

1. **C3** — ერთი სტრიქონი: `PATCH /api/me/tutor`-ში დაუშვი `REDIRECTED`, რომლის მშობელიც `VISIBLE`-ია (ზუსტად `BROWSABLE_CATEGORY`). ახლა 7 კატეგორია მთელ პროფილის შენახვას ვარდნის.
2. **C1** — გადაწყვიტე: `finance` → `business`, თუ სფეროების სახელები შეიცვალოს. დღეს ჩიპები ცრუობს.
3. **H4** — ორი გაფრთხილების ტექსტი გაასწორე (ან, თუ კატეგორია მართლაც სავალდებულოდ გინდა, `tutorsQuery` შეცვალე — მაგრამ ერთი მათგანი მაინც უნდა შეიცვალოს).
4. **H1 + ადმინის ხარვეზი** — დაამატე კატეგორიის ცვლილება ადმინის ექსპერტის დრაუერში; ნინო გახოკია გადაიტანე ფსიქოლოგიაში.
5. **C2** — ან `profileType`-ის დაყენება (ადმინის toggle რომ ორივეს წერდეს), ან `/swavleba` და პაკეტების gate მხოლოდ `packagesEnabled`-ზე; და seed-ჩანაწერი პროდაქშენიდან.
6. **H2** — `orderBy` დაამატე, `HIDDEN`-იდან `diaspora` გამორიცხე, ფუძის არმი შეამოწმე ან მოხსენი.
7. **H3 / M1 / M4** — ერთი გადაწყვეტილება: `specialty` ან საჯარო ველია და სინქრონდება, ან შიდაა და არსად არ იხატება. დღეს ის ნახევრად საჯაროა და გაყინული.
